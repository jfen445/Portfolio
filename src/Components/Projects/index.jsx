import * as React from 'react';

const Projects = ({ scrollRef, scrollToDiv }) => {
  return (
    <div className="container" ref={scrollRef}>
      <div className="left project-bg">
        <section>hello</section>
      </div>
    </div>
  );
};

export default Projects;
